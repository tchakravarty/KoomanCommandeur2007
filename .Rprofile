# load all the packages
library(zoo)
library(ggplot2)
library(gridExtra)
library(qwraps) # github
library(KFAS)
library(lmtest)
library(scales)

# set the default plotting theme
theme_set(theme_bw())

#================================================
# Goldfeld-Quandt test
#================================================
fnGQ = function(vResid) {
  # TODO
}